#The socket connects at the top layer, using a specific port number
#HTTP is the dominant Application layer protocol 
#A protocol is a set of rules that all parties follow so they can predict each others behaviour
